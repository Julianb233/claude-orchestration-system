# Commit

Smart commit with auto-generated message.

1. Stage all changes: `git add -A`
2. Generate commit message from diff
3. Show preview:
   ```
   Commit: feat: add user authentication

   Files: 5 changed

   [Enter] to commit, or type custom message:
   ```
4. Commit with message
5. Suggest push
