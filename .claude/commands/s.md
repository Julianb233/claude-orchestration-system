# Shortcut for /sync

Check for pending file sync updates from the File Watcher Agent.

Run the full `/sync` command logic - see `/root/.claude/commands/sync.md` for details.

Quick check: Read `/root/.claude-flow/sync/pending-updates.jsonl` and process any pending notifications.
