# New Branch

Create a new feature branch.

Usage: `/new feature-name` or just `/new`

1. If name provided, use it
2. Otherwise, ask what you're building
3. Generate branch name: `feature/`, `fix/`, `chore/`
4. Create and checkout:
   ```bash
   git checkout -b {branch-name}
   ```
5. Confirm and suggest next steps
