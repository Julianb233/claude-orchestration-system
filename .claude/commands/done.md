# Done

Mark current work complete and wrap up.

1. Run checks (`/ok`)
2. Commit if uncommitted changes (`/cm`)
3. Push (`/p`)
4. Create PR if on feature branch (`/pr`)
5. Save to history (`/h`)
6. Clear state file

One command to ship it all.
