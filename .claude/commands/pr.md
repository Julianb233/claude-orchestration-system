# Create Pull Request

Create a PR for the current branch.

1. Check if branch is pushed
2. Generate PR title from branch name or recent commits
3. Generate PR body with summary of changes
4. Run: `gh pr create --fill` or with generated content
5. Show PR URL
