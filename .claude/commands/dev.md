# App Development Shortcut

Shortcut for `/app-dev`. See full documentation in `/app-dev`.

Load knowledge base at `/root/.claude/knowledge/julian-stack.md` and help with app development.

## Requirements

$ARGUMENTS
